# HW4 — Regularization (Ridge/Lasso) + PCA

Compared ridge and lasso regularization for predicting orange juice brand choice (CH vs MM), selecting a model based on performance and interpretability. Also applied PCA to the USArrests dataset to reduce correlated features and interpret variance explained by principal components.
