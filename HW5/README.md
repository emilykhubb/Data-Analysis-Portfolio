# HW5 — Supervised ML Model Comparison

Benchmarked multiple supervised classification models on the OJ dataset (CH vs MM), including trees, bagging, random forests, boosting, and SVMs. Compared models using test-set performance and confusion matrices to identify the most effective approach.
